// Jest mock for static assets like CSS/HTML.
// We export an object with a `default` string so that both
// CommonJS `require` and ESModule default imports work without errors.
module.exports = {
  default: '',
};
